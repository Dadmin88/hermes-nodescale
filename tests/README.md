# Workspace test layout

Crate integration tests are colocated under each crate's `tests/` directory. This workspace-level directory is reserved for future cross-crate fixtures that do not require live infrastructure.
